## What are the defferent Method have you used in TPL ?

TPL is a set of types and APIs across `System.Threading` and `System.Threading.Tasks`. The most commonly used methods are in `Task` and `Parallel`

## Common Task methods   
Task.Run(...)

Task.Delay(...)

Task.WhenAll(...)

Task.WhenAny(...)

Task.Wait()

Task.WaitAll(...)

Task.WaitAny(...)

Task.ContinueWith(...)

Task.FromResult(...)

Task.FromException(...)

Task.FromCanceled(...)

## Common Parallel methods   
Parallel.For(...)

Parallel.ForEach(...)

Parallel.Invoke(...)
