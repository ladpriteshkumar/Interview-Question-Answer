# How do you implement model validation in asp net core ?

To implement model validation in ASP.NET Core, you primarily use `Data Annotations` to decorate your model properties with validation rules.

Alternatively ,we can also use   `FluentValidation` because it’s cleaner and more powerful.
